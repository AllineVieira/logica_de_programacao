//Leia um vetor de 5 posições contendo os caracteres de um numero. Em seguida escreva esse numero por extenso.

let arr = ['C', 'i', 'n', 'c', 'o'];

let arrNumber = arr.join("");

console.log(arrNumber);